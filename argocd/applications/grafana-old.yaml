apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-old
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/distributed-tracing-python-kubernetes
    targetRevision: HEAD
    path: charts/grafana-OLD
  destination:
    server: https://kubernetes.default.svc
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
